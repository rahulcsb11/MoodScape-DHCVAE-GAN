# MoodScape — DH-CVAE-GAN (Reference Mini-Release)

This repository provides a minimal, runnable reference for the Dual-Head Conditional VAE-GAN (DH-CVAE-GAN) used in *“MoodScape: Emotion-Driven Terrain Synthesis for Virtual Reality”*.

## Quickstart (smoke test — no dataset needed)
```bash
pip install -r requirements.txt
python moodscape_smoke.py
